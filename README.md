## Chess engine + GUI + simple AI


Install via yarn:
```bash
yarn install

```

Install via npm:

```bash
npm install
```


Run:

```bash
npm run build # Run `npm run build:watch` to compile after each change.
npm run start
```

