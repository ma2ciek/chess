Chess engine + GUI + simple AI.


Install via yarn:
```bash
yarn install

```

Install with npm:

```bash
npm install
```


Run:

```bash
npm run build #or `npm run build:watch` to compile after each change.
npm run start
```

